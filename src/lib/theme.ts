// Re-export from kernel — theme is core infrastructure, not a library utility.
// Kept here for backward compatibility with existing layout/page imports.
export { generateCssVars } from '../../kernel/theme'
