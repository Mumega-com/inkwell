Tile components dispatched from `pages/dashboard.astro`. Each file handles one `TileTemplate` kind from the Glass service contract.

Add new template kinds by creating a new `.astro` file here and wiring it in the `switch` block inside `dashboard.astro`.
